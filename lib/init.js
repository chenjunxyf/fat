require('colors');

module.exports = {
    /**
     * 脚手架初始化
     * @param  {[String]} type        [模板类型]
     * @param  {[String]} localTplDir [本地模板路径]
     */
    init: function(type, localTplDir) {
        console.log(type);
        console.log(localTplDir);
    }
};